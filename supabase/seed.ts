// Supabase seed file - migrations own the schema creation.

export default async function seed(_client: unknown) {
  void _client;
  console.log("Seeding database...");
  console.log(
    "Seed complete - migrations should have created logisticas_entrega table"
  );
}
